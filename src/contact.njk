---
layout: layout.njk
title: "Contact | Altaris Growth Partners"
description: "Get in touch with Altaris Growth Partners to discuss your business and schedule a free strategic assessment."
permalink: "/contact/"
schema1: |
  {"@context":"https://schema.org","@type":"BreadcrumbList","itemListElement":[{"@type":"ListItem","position":1,"name":"Home","item":"https://altarisgrowth.com/"},{"@type":"ListItem","position":2,"name":"Contact"}]}
schema2: |
  {
    "@context": "https://schema.org",
    "@type": "ContactPage",
    "name": "Contact Altaris Growth Partners",
    "about": {"@type":"Organization","name":"Altaris Growth Partners Inc.","email":"hello@altarisgrowth.com","telephone":"+1-416-555-0142"}
  }
---
<section class="page-hero parallax-section">
  <div class="photo-layer parallax-layer" data-speed="0.12" style="background-image:url('https://picsum.photos/seed/altaris-contact/1800/800');"></div>
  <div class="photo-tint brass-edge"></div>
  <div class="container">
    <p class="breadcrumb"><a href="/">Home</a> / <span>Contact</span></p>
    <p class="eyebrow">{{ pages.contact.eyebrow }}</p>
    <h1>{{ pages.contact.h1 }}</h1>
    <p class="lede">{{ pages.contact.lede }}</p>
  </div>
</section>

<section class="contact parallax-section">
  <div class="photo-layer parallax-layer" data-speed="0.12" style="background-image:url('https://picsum.photos/seed/altaris-contact/1800/1100');"></div>
  <div class="photo-tint"></div>
  <div class="container">
    <div class="contact-grid">
      <div class="contact-info reveal">
        <p class="eyebrow" style="color:var(--brass-deep);">{{ pages.contact.reply_eyebrow }}</p>
        <h2 style="color:var(--ink);">{{ pages.contact.reply_heading }}</h2>
        <p>{{ pages.contact.reply_note }}</p>
        <div class="contact-detail">
          <div>{{ contactInfo.email }}</div>
          <div>{{ contactInfo.phone }}</div>
          <div>{{ contactInfo.location }}</div>
        </div>
      </div>

      <!--
        Netlify Forms: detected automatically at build time because of
        name + data-netlify="true". No external service, no API key.
        Submissions land in Site configuration -> Forms in the Netlify
        dashboard, and email notifications can be turned on there too
        (Notifications -> Email notification -> Form submission).
      -->
      <form class="contact-form reveal" id="contactForm" name="contact" method="POST" data-netlify="true" netlify-honeypot="bot-field">
        <input type="hidden" name="form-name" value="contact">
        <p class="hp-field"><label>Leave this empty: <input name="bot-field"></label></p>
        <input type="text" name="name" placeholder="Full name" aria-label="Full name" required>
        <input type="email" name="email" placeholder="Work email" aria-label="Work email" required>
        <input type="text" name="company" placeholder="Company name" aria-label="Company name">
        <textarea name="message" placeholder="What's the current state of the business?" aria-label="Message" required></textarea>
        <button type="submit" class="btn-primary">Send message</button>
        <div class="form-status" id="formStatus" role="status" aria-live="polite"></div>
        <p class="site-footer-note">We reply within one business day. Your information is never shared.</p>
      </form>
    </div>
  </div>
</section>
